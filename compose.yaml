services:
  app:
    build:
      context: .
    image: ${DOCKER_IMAGE}
    ports:
      - "${SPRINGDRASIL_PORT}:8080"
    environment:
      DB_URL: ${DB_URL}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      - postgres

  postgres:
    image: postgres:18.4
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres18-data:/var/lib/postgresql

volumes:
  postgres18-data:
